{
  "name": "apple-notes-mcp",
  "version": "1.0.0",
  "author": "",
  "type": "module",
  "main": "index.ts",
  "description": "",
  "keywords": [],
  "license": "ISC",
  "scripts": {
    "build": "bun build index.ts --outdir dist --target node",
    "start": "bun index.ts",
    "purge-db": "rm -rf ~/.mcp-apple-notes",
    "test": "npx tsx test/test-search.mjs 2>&1 | grep -q '0 failed' || exit 1; for f in test/test-*.mjs; do [ \"$f\" = test/test-search.mjs ] && continue; node --experimental-vm-modules \"$f\" || exit 1; done"
  },
  "exports": {
    ".": "./index.ts"
  },
  "dependencies": {
    "@huggingface/transformers": "^3.1.2",
    "@lancedb/lancedb": "^0.14.0",
    "@langchain/textsplitters": "^0.1.0",
    "@modelcontextprotocol/sdk": "^1.0.3",
    "run-jxa": "^3.0.0",
    "turndown": "^7.2.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/turndown": "^5.0.5",
    "prettier": "^3.8.1",
    "tsx": "^4.21.0",
    "typescript": "^6.0.2"
  }
}
